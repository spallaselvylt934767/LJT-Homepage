# Authors

# The author profile shown in the sidebar is configured in _config.yml
# under the `author` key. Add extra named authors here only if a page or
# post sets an `author:` field in its front matter.